mixin blockList(block)
  div(class=`blocks blocks--${block.modifier}`)
    for item in block.items
      .col-xs-12.col-lg-4
        div(class='blocks__item')
          h3(class=`blocks__title blocks__title--${block.titleSize}`)= item.title
          p.blocks__description= item.description
  
